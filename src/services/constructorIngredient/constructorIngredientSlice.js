import {createSlice} from "@reduxjs/toolkit";


const initialState = {
  constructorIngredient: null,
}

export const constructorIngredientSlice = createSlice({
  name: 'constructorIngredient',
  initialState,
  reducers: {},
  extraReducers: {
  
  }
})
